using System;
using System.IO;

namespace TotalyNotAVirus
{
    internal class Program
    {
        static void Main(string[] args)
        {
            while(true)
            {
                using (FileStream stream = new FileStream(@$"C:).jpeg", FileMode.CreateNew))
                {

                }
            }

        }
    }
}
